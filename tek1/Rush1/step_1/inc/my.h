#ifndef PLAT_H_
# define PLAT_H_

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <errno.h>
#include <string.h>
# define ABS(Value) ((Value < 0) ? (Value * -1) : (Value))

#endif /* !PLAT_H_ */
